interface ThemeStructure {
  colors: {
    mainColor: string;
  };
  fonts: {
    mainFontFamily: string;
    mainFontSize: string;
    biggerFontSize: string;
  };
  sizes: {
    mobileWidth: string;
  };
}

export default ThemeStructure;
